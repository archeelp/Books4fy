<!DOCTYPE html>
<html>
    <head>
        <title>Books for Fy</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <link href="https://fonts.googleapis.com/css?family=Courgette&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=DM+Serif+Text&display=swap" rel="stylesheet">
    </head>
    <style>
        #logo {
            font-family: 'Courgette', cursive;
            font-size: 150%;
        }
        a {
            font-size: 120%;
        }
    </style>
    <body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a id="logo" class="navbar-brand" href="/">books4fy</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="/books">Books</a></li>
                    <% if(!currentuser){ %>
                    <li><a href="/signin">Login</a></li>
                    <li><a href="/signup">Sign-up</a></li>
                    <% } else { 
                        if(currentuser.type == "sy"){ %>
                            <li><a href="/books/<%=currentuser._id%>">Signed In As <%= currentuser.name %></a></li>
                            <li><a href="/srequests">My requests</a></li>
                            <li><a href="/end">End Selling</a></li>
                            <% if(!currentuser.description){ %>
                                <li><a href="/adddetails %>">Add Details</a></li>
                                <% } %>
                        <% } 
                        if(currentuser.type == "fy"){ %>
                            <li><a href="#">Signed In As <%= currentuser.name %></a></li>
                            <li><a href="/requests">My requests</a></li>
                        <% } 
                        if(currentuser.type == "admin"){ %>
                            <li><a href="#">Signed In As Admin</a></li>
                            <li><a href="/admin">Feedbacks</a></li>
                        <% } %>
                    <li><a href="/logout">Logout</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container">
        
        <% if(error && error.length > 0){ %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>
        <% if(success && success.length > 0){ %>
            <div class="alert alert-success" role="alert">
                <%= success %>
            </div>
        <% } %>
        <% if(warning && warning.length > 0){ %>
            <div class="alert alert-warning" role="alert">
                <%= warning %>
            </div>
        <% } %>
        <% if(info && info.length > 0){ %>
            <div class="alert alert-info" role="alert">
                <%= info %>
            </div>
        <% } %>
    </div>